import { useMemo, useState } from 'react';
import { Eye, MoreVertical, Printer, RotateCcw, Search } from 'lucide-react';
import { useNavigate } from 'react-router-dom';
import { PageHead, Status } from './components';
import { departmentNames } from './data';
import { label, useStore } from './store';

const PAGE_SIZE = 8;

export function AllCorrespondence() {
  const mail = useStore((s) => s.mail), navigate = useNavigate();
  const [query,setQuery]=useState(''),[type,setType]=useState(''),[status,setStatus]=useState(''),[priority,setPriority]=useState(''),[department,setDepartment]=useState(''),[from,setFrom]=useState(''),[to,setTo]=useState(''),[sort,setSort]=useState<'new'|'old'>('new'),[page,setPage]=useState(1);
  const rows=useMemo(()=>mail.filter(m=>!m.archived).filter(m=>!query||[m.number,m.subject,m.from,m.to,m.body||''].some(value=>value.includes(query))).filter(m=>!type||m.type===type).filter(m=>!status||m.status===status).filter(m=>!priority||m.priority===priority).filter(m=>!department||m.department===department).filter(m=>!from||m.date>=from).filter(m=>!to||m.date<=to).sort((a,b)=>sort==='new'?b.date.localeCompare(a.date):a.date.localeCompare(b.date)),[mail,query,type,status,priority,department,from,to,sort]);
  const pages=Math.max(1,Math.ceil(rows.length/PAGE_SIZE)),visible=rows.slice((Math.min(page,pages)-1)*PAGE_SIZE,Math.min(page,pages)*PAGE_SIZE);
  const reset=()=>{setQuery('');setType('');setStatus('');setPriority('');setDepartment('');setFrom('');setTo('');setPage(1)};
  return <><PageHead title="كل المراسلات" subtitle="سجل موحد للبحث والمتابعة عبر الوارد والصادر والمراسلات الداخلية" action="إنشاء مراسلة" to="/app/compose/outgoing"/><section className="panel correspondence-filters"><label className="correspondence-search"><Search/><input value={query} onChange={e=>{setQuery(e.target.value);setPage(1)}} placeholder="ابحث بالرقم أو الموضوع أو الجهة أو كلمات من النص..."/></label><select value={type} onChange={e=>setType(e.target.value)}><option value="">كل الأنواع</option><option value="incoming">وارد</option><option value="outgoing">صادر</option><option value="internal">داخلي</option></select><select value={status} onChange={e=>setStatus(e.target.value)}><option value="">كل الحالات</option>{[...new Set(mail.map(m=>m.status))].map(value=><option key={value}>{value}</option>)}</select><select value={priority} onChange={e=>setPriority(e.target.value)}><option value="">كل الأولويات</option><option>عادي</option><option>عاجل</option><option>عاجل جداً</option></select><select value={department} onChange={e=>setDepartment(e.target.value)}><option value="">كل الأقسام</option>{departmentNames.map(value=><option key={value}>{value}</option>)}</select><label><span>من</span><input type="date" value={from} onChange={e=>setFrom(e.target.value)}/></label><label><span>إلى</span><input type="date" value={to} onChange={e=>setTo(e.target.value)}/></label><select value={sort} onChange={e=>setSort(e.target.value as 'new'|'old')}><option value="new">الأحدث أولًا</option><option value="old">الأقدم أولًا</option></select><button className="secondary" onClick={reset}><RotateCcw/> مسح التصفية</button></section><section className="panel correspondence-table-card"><div className="panel-head"><div><h2>نتائج المراسلات</h2><p>{rows.length} مراسلة مطابقة</p></div></div><div className="correspondence-table-wrap"><table className="correspondence-table"><thead><tr><th>رقم المراسلة</th><th>الموضوع والجهة</th><th>النوع</th><th>القسم الحالي</th><th>الأولوية</th><th>التاريخ</th><th>الحالة</th><th>الإجراءات</th></tr></thead><tbody>{visible.map(m=><tr key={m.id}><td><b>{m.number}</b></td><td><strong>{m.subject}</strong><small>{m.type==='outgoing'?m.to:m.from}</small></td><td>{label(m.type)}</td><td>{m.department||'غير محدد'}</td><td><span className={`priority p-${m.priority.replaceAll(' ','-')}`}>{m.priority}</span></td><td>{m.date}</td><td><Status>{m.status}</Status></td><td><details className="row-actions"><summary aria-label={`إجراءات ${m.number}`}><MoreVertical/></summary><div><button onClick={()=>navigate(`/app/mail/${m.id}`)}><Eye/> عرض التفاصيل</button><button onClick={()=>{navigate(`/app/mail/${m.id}`);setTimeout(()=>window.print(),400)}}><Printer/> طباعة</button></div></details></td></tr>)}</tbody></table></div>{!visible.length&&<div className="correspondence-no-results"><Search/><h3>لا توجد نتائج مطابقة للبحث</h3><button className="secondary" onClick={reset}>مسح الفلاتر</button></div>}{rows.length>PAGE_SIZE&&<div className="pagination"><button disabled={page<=1} onClick={()=>setPage(page-1)}>السابق</button><span>صفحة {Math.min(page,pages)} من {pages}</span><button disabled={page>=pages} onClick={()=>setPage(page+1)}>التالي</button></div>}</section></>;
}
