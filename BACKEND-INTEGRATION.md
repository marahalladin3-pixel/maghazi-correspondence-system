# ربط نظام المراسلات والأرشيف الإلكتروني

## حدود الموديول داخل موقع البلدية

- إدارة المستخدمين والحسابات والصلاحيات تبقى في النظام الأساسي للبلدية ولا تُكرّر داخل موديول المراسلات.
- يستقبل الموديول المستخدم الحالي ودوره وقسمه وصلاحياته من جلسة النظام الأساسي أو من تسجيل الدخول الموحّد.
- دليل الموظفين والأقسام المستخدم في التحويل والتوجيه يُقرأ من النظام الأساسي عبر API، ولا يُعدّل من داخل الموديول.
- إشعارات المراسلات تُنشأ في الموديول وتظهر من خلال مركز إشعارات الموقع الأساسي أو واجهة الإشعارات المشتركة.

## دورة التأشير والمتابعة

يجب أن يحفظ الخادم مع كل تحويل: نوع المستلم (موظف أو قسم)، المستلم، الإجراء المطلوب، صلاحية الاطلاع، آخر موعد، هل يتطلب ردًا، التذكير، الجهات المنسوخ لها، والمراسلات المرتبطة. كل تحويل أو إعادة تحويل أو تمديد أو إرجاع أو تنفيذ يُضاف كسجل مستقل غير قابل للتعديل في سجل حركة المراسلة، ثم يُرسل إشعارًا للمستلم.

الحالات التشغيلية المقترحة للصندوق الشخصي: جديد، غير مقروء، يتطلب إجراء، قيد المعالجة، بانتظار الرد، متأخر، مُعاد، تم الإنجاز، ومؤرشف. تُحسب حالة التأخير من آخر موعد على الخادم وليس من واجهة المستخدم.

الواجهة جاهزة للدمج داخل بوابة بلدية المغازي. لكي تصبح تشغيلية ومعتمدة، يضبط المهندس `MunicipalityConfig.useBackend = true` ويحدد `apiBase` ثم يربط المسارات التالية:

- `POST /api/auth/login` و`POST /api/auth/logout`
- `GET/POST /api/correspondence/incoming`
- `GET/POST /api/correspondence/outgoing`
- `GET/POST /api/correspondence/internal`
- `GET/PATCH /api/correspondence/{id}`
- `POST /api/correspondence/{id}/transfer`
- `POST /api/correspondence/{id}/notes`
- `POST /api/correspondence/{id}/complete`
- `POST /api/correspondence/{id}/archive`
- `POST /api/correspondence/{id}/attachments`
- `GET /api/archive/search`
- `GET /api/reports`
- `GET /api/audit-log`
- `GET /api/notifications` و`PATCH /api/notifications/{id}/read`
- `POST /api/correspondence/{id}/submit-review`
- `POST /api/correspondence/{id}/review`
- `POST /api/correspondence/{id}/approve`
- `POST /api/correspondence/{id}/return-for-edit`
- `POST /api/correspondence/{id}/extension-requests`
- `PATCH /api/extension-requests/{id}`
- `GET/POST /api/correspondence/{id}/versions`
- `POST /api/scans/batches` و`GET /api/scans/batches/{id}`
- `POST /api/scans/batches/{id}/process` لدمج PDF وOCR
- `GET /api/registry/duplicates` لفحص التكرار قبل القيد

## متطلبات الاعتماد

- صلاحيات حسب الدور والقسم، ومنع الوصول على مستوى الخادم.
- ترقيم ذري لا يتكرر للوارد والصادر لكل سنة.
- سجل عمليات غير قابل للتعديل يسجل المستخدم والوقت وعنوان الجهاز والإجراء.
- تخزين المرفقات خارج المجلد العام، مع فحص النوع والحجم والبرمجيات الضارة.
- بصمة SHA-256 لكل ملف، وسياسة احتفاظ ونسخ احتياطي واختبار استعادة.
- HTTPS، جلسات آمنة، حماية CSRF، وتحديد محاولات الدخول.
- قاعدة بيانات معاملاتية وفهارس للرقم والتاريخ والموضوع والجهة والقسم.
- توليد PDF رسمي وختم أو توقيع إلكتروني إذا اعتمدته البلدية قانونياً.

التخزين المحلي الحالي لتجربة الواجهة فقط، وليس بديلاً عن قاعدة بيانات البلدية.

## تفعيل الاتصال

يُعرّف الإعداد قبل تحميل `municipality-api.js`:

```html
<script>
window.MunicipalityConfig = {
  apiBase: 'https://municipality.example',
  useBackend: true
};
</script>
```

تعتمد الواجهة جلسة البوابة الحالية باستخدام `credentials: include`. لا تحتوي الواجهة على كلمات مرور ثابتة أو صلاحيات أمنية محلية. يجب أن يعيد الخادم المستخدم الحالي وصلاحياته، وأن يرفض كل إجراء غير مصرح به حتى لو تم استدعاؤه خارج الواجهة.

حقول المراسلة الأساسية المتوقعة: `id`, `number`, `date`, `kind`, `subject`, `status`, `priority`, `confidentiality`, `department`, `employee`, `attachments`, `activity`, `createdAt`, `updatedAt`.

## ضوابط إنتاج إضافية

- يصدر الخادم أرقام الوارد والصادر داخل معاملة قاعدة بيانات وقيد فريد؛ الرقم المقترح في الواجهة ليس مرجعًا رسميًا.
- كل اعتماد أو إعادة أو تمديد يحفظ صاحب القرار ووقته وملاحظته ولا يسمح بتعديل الحدث بعد إنشائه.
- كل ملف يمر بفحص برمجيات ضارة، والتحقق من النوع الحقيقي، وحساب بصمة SHA-256 قبل إتاحته.
- الإصدارات النهائية تحفظ ككائنات غير قابلة للاستبدال، ويُنشأ إصدار جديد لأي تعديل.
- خدمة المسح المحلية تعمل على `127.0.0.1` فقط، تستخدم رمزًا قصير العمر، وتتحقق من أصل البوابة المسموح.
- OCR ودمج PDF عمليات خلفية لها حالات: `queued`, `processing`, `completed`, `failed` مع إعادة محاولة مقيدة.
- يُختبر النسخ الاحتياطي باستعادة فعلية دورية، وتراقب أخطاء API والتخزين والمهام الخلفية.
